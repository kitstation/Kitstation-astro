---
import BaseLayout from "../layouts/BaseLayout.astro";
import Header from "../components/Header.astro";
import MarqueeBar from "../components/MarqueeBar.astro";
import Footer from "../components/Footer.astro";
import WhatsAppButton from "../components/WhatsAppButton.astro";
import "../styles/global.css";
---

<BaseLayout title="Contáctanos | KitStation" description="Habla con el equipo de KitStation para descubrir la solución ideal para tu negocio.">
  <Header />
  <MarqueeBar />
  <main class="contact-page">
    <section class="contact-page__section">
      <div class="container contact-page__grid">
        <div class="contact-page__copy">
          <h1>
            <span>Habla con nuestro equipo</span>
            <span>de ventas para descubrir</span>
            <span>cómo KitStation se adapta</span>
            <span>a tus necesidades</span>
          </h1>

          <ul class="contact-page__benefits">
            <li>Encuentra la solución perfecta para impulsar el crecimiento de tu negocio.</li>
            <li>Pide una cotización adaptada a tus necesidades.</li>
            <li>Aceleramos el crecimiento digital de tu empresa desde el primer mes.</li>
            <li>Creamos experiencias digitales que convierten visitantes en clientes.</li>
          </ul>

          <blockquote class="contact-page__quote">
            “Despreocúpate del contenido digital. En KitStation nos encargamos de todo, para<br />
            que tú puedas enfocarte en hacer crecer tu negocio.”
          </blockquote>
        </div>

        <div class="contact-page__panel">
          <form class="contact-page__form" method="POST" action="/api/enviar-formulario" data-mail-form>
            <h2>Comunícate con nuestro equipo de ventas</h2>

            <input type="hidden" name="formulario" value="Contacto" />
            <input type="hidden" name="pagina_origen" value="" />
            <input class="form-honeypot" type="text" name="website" autocomplete="off" tabindex="-1" aria-hidden="true" />
            <input type="text" name="name" placeholder="Nombre" required />
            <input type="tel" name="phone" placeholder="Celular" required />
            <input type="email" name="email" placeholder="E-mail" required />

            <label class="contact-page__label" for="service">¿Qué te gustaría hablar con nosotros?</label>
            <select id="service" name="service" required>
              <option>Página web</option>
              <option>Animación digital</option>
              <option>Automatización con IA</option>
              <option>Campañas digitales</option>
              <option>Posicionamiento SEO</option>
              <option>Consultoría y soporte técnico</option>
              <option>Creación de contenido</option>
            </select>

            <textarea name="message" placeholder="Déjanos tu consulta..." required></textarea>

            <label class="contact-page__check">
              <input type="checkbox" name="privacy" required />
              <span>Acepto la Política de privacidad de kitstation.com.</span>
            </label>

            <button type="submit">ENVIAR CONSULTA</button>
            <p class="form-feedback" role="status" aria-live="polite"></p>
          </form>
        </div>
      </div>
    </section>
  </main>
  <Footer />
  <WhatsAppButton />
</BaseLayout>

<style>
  .contact-page {
    min-height: 100vh;
    background: #fff;
  }

  .contact-page__section {
    padding: 78px 0 94px;
  }

  .contact-page__grid {
    display: grid;
    grid-template-columns: minmax(0, 1.05fr) minmax(420px, 0.95fr);
    gap: 56px;
    align-items: center;
  }

  .contact-page__copy {
    display: grid;
    justify-items: start;
    align-content: center;
    gap: 26px;
    padding: 20px 0;
    text-align: left;
  }

  .contact-page__copy h1 {
    max-width: none;
    margin: 0;
    color: #000;
    font-size: clamp(2.1rem, 3.2vw, 3.35rem);
    font-weight: 600;
    line-height: 0.94;
    letter-spacing: -0.06em;
  }

  .contact-page__copy h1 span {
    display: block;
    white-space: nowrap;
  }

  .contact-page__benefits {
    display: grid;
    gap: 18px;
    margin: 0;
    padding: 0;
    width: min(100%, 680px);
    list-style: none;
  }

  .contact-page__benefits li {
    position: relative;
    padding-left: 30px;
    color: #627084;
    font-size: 1.02rem;
    line-height: 1.45;
    text-align: left;
  }

  .contact-page__benefits li::before {
    content: "✓";
    position: absolute;
    left: 0;
    top: 1px;
    color: #1c54f8;
    font-size: 1rem;
    font-weight: 700;
  }

  .contact-page__quote {
    margin: 4px 0 0;
    max-width: 740px;
    padding-top: 18px;
    border-top: 1px solid rgba(13, 22, 47, 0.08);
    color: #0d162f;
    font-size: clamp(0.92rem, 1.2vw, 1.08rem);
    line-height: 1.45;
    text-align: center;
  }

  .contact-page__panel {
    padding: 34px;
    border-radius: 28px;
    background: #fff;
    box-shadow: 0 24px 68px rgba(9, 18, 37, 0.08);
  }

  .contact-page__form {
    display: grid;
    gap: 14px;
  }

  .contact-page__form h2 {
    margin: 0 0 6px;
    color: #0d162f;
    font-size: clamp(1.5rem, 2vw, 2rem);
    line-height: 1.02;
  }

  .contact-page__form input,
  .contact-page__form select,
  .contact-page__form textarea {
    width: 100%;
    border: 1px solid rgba(13, 22, 47, 0.12);
    border-radius: 14px;
    background: #fff;
    color: #111827;
    padding: 14px 16px;
  }

  .contact-page__form textarea {
    min-height: 140px;
    resize: vertical;
  }

  .contact-page__label {
    color: #0d162f;
    font-size: 0.95rem;
    font-weight: 600;
  }

  .contact-page__check {
    display: flex;
    align-items: flex-start;
    gap: 10px;
    color: #5f6674;
    font-size: 0.92rem;
  }

  .contact-page__check input {
    width: auto;
    margin-top: 4px;
  }

  .contact-page__form button {
    min-height: 52px;
    border: 0;
    border-radius: 999px;
    background: #1c54f8;
    color: #fff;
    font-weight: 700;
    cursor: pointer;
  }

  @media (max-width: 960px) {
    .contact-page__grid {
      grid-template-columns: 1fr;
    }

    .contact-page__copy {
      justify-items: start;
      gap: 22px;
      padding: 0;
      text-align: left;
    }

    .contact-page__benefits {
      width: 100%;
    }

    .contact-page__quote {
      text-align: left;
    }

    .contact-page__panel {
      padding: 26px;
    }
  }
</style>
